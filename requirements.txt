# Used by the Streamlit dashboard (Streamlit Community Cloud installs this).
# Keep it minimal — the app only needs these two. Scanner deps live in
# requirements-scan.txt (installed by the GitHub Action).
streamlit>=1.36
requests>=2.31
